<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <title>CheapSGCarRental</title>
    <style>
        body{
        background-color:grey;
        padding-top:30px;
      }
      a:hover, a:active{
        color: orange !important;
      }
    </style>
</head>
<body>
    <%- include ('partials/navbar.ejs') %>
    <!-- car details -->
        <div class="container">
            <br>
            <div class="text-center"><h2>Car Details</h2></div>
            <br>
            <div class="text-center">
                <% if (car) { %> <!--check if car data exists-->
                    <p><b>Car Image:</b><br> <img src="<%= car.image %>" width="20%"></p> 
                    <p><b>Brand:</b><br> <%= car.brand %></p>
                    <p><b>Model:</b><br> <%= car.model %></p>
                    <p><b>Category:</b><br> <%= car.category %></p>
                    <p><b>Seats:</b><br> <%= car.seats %></p>
                    <p><b>Engine Capacity(CC):</b><br> <%= car.cc %></p>
                    <p><b>Transmission:</b><br> <%= car.transmission %></p>
                    <p><b>Rental:</b><br>$ <%= car.rental %></p>
                <% } else{ %>
                    <p>No Car Found.</p>
                <% } %>
                <a href="/">Back</a>
            </div>
        </div>


</body>
</html>